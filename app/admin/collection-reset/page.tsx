import AdminPlaceholder from "@/components/AdminPlaceholder";

export const metadata = {
  title: "수집 상태 초기화 · CS-G2B",
};

export default function AdminCollectionResetPage() {
  return (
    <AdminPlaceholder
      title="수집 상태 초기화"
      description="snapshot / 캐시 / 신규 표시 reset — 다음 단계에서 통합 UI 추가."
    />
  );
}
